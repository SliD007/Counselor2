<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="test.example.com.counselor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".view.login.LoginActivity" android:theme="@style/TranslucentTheme" android:launchMode="singleTask"/>
        <activity android:name=".view.backlog.BacklogActivity" android:theme="@style/TranslucentTheme" android:launchMode="singleTask"/>
        <activity android:name=".view.schedule.ScheduleActivity" android:theme="@style/TranslucentTheme" android:launchMode="singleTask"/>
        <activity android:name=".view.service.ServiceActivity" android:theme="@style/TranslucentTheme" android:launchMode="singleTask"/>
        <activity android:name=".view.personal.PersonalActivity" android:theme="@style/TranslucentTheme" android:launchMode="singleTask"/>
    </application>

</manifest>